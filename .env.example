# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:9001/api/v1

# Base URL for media stored in S3 (used by resolveMediaUrl for relative paths)
NEXT_PUBLIC_S3_BASE_URL=https://pedator-backet.s3.ap-south-1.amazonaws.com

# Legacy fallback (kept for backward compatibility)
NEXT_PUBLIC_CLOUDFRONT_BASE_URL=
